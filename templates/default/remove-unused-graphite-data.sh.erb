#!/bin/sh

find <%= node["graphite-tools"]["path"]["whisper"] %> -type f -mtime +<%= node["graphite-tools"]["cleanup"]["days_before"] %> -name \*.wsp -delete
find <%= node["graphite-tools"]["path"]["whisper"] %> -depth -type d -empty -delete
